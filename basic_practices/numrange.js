function numberRange(num) {
  switch (num) {
    case num <= 50 :
      console.log(`${num} is between 0 and 50`);
      
    case num <= 100 :
      console.log(`${num} is between 51 and 100`);
      
  }
}

numberRange(20);
numberRange(55);