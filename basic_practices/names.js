let names = ['chris', 'kevin', 'pete'];
let upcaseNames = [];
let index = 0;

while (index < names.length) {
  upcaseNames.push(names[index].toUpperCase());
  index += 1;
}

console.log(upcaseNames);
