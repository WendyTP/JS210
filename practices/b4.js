/*
yes, because array is actually an object. The negative index and the value it associate
is just a property. The value however, is not considered as element and the negative index
is not really considered as index for array though.
*/
